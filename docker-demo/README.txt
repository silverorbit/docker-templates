This Docker setup versions the app with the server. 
See the Dockerfile image is nginx.

When build is issued it creates a docker images called so_app tagged as v1.0.0.

DEV to PRODUCTION setup as show here. https://www.youtube.com/watch?v=LSyIE-bTt5U
